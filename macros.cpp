#include "bits/stdc++.h"
using namespace std;
#define endl "\n"
#define endly " "
#define int long long
#define fr(i, a, b) for (ll i = a; a < b ? i <= b : i >= b; a < b ? i++ : i--)
#define sz(x) ((int)x.size())
#define len(x) ((int)x.length())
#define F first
#define S second
#define all(x) begin(x), end(x)

int32_t main()
{
      ios::sync_with_stdio(false);
      cin.tie(0); 
      return 0;
}
